using UnityEngine;
using System.Collections;

namespace AngryRain.Vehicles {
	public class GroundVehicleController : MonoBehaviour {

		// Use this for initialization
		void Start () {
		
		}
		
		// Update is called once per frame
		void Update () {
			Drive();
		}

		void Drive(){
			//if(Input.GetButton (AngryRain.
		}
	}
}
